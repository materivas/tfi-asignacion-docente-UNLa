package com.gestion.backend.model;

// DOC: [EV-20] Creación de Enum RoleType para soportar el modelo de control de accesos basado en roles (RBAC).
public enum RoleType {
    ROLE_ADMIN,
    ROLE_DOCENTE
}